export default function Placeholder({ title }) {
  return (
    <div className="placeholder-page">
      <h2>{title}</h2>
      <p>Essa tela ainda vai ser construída nos próximos passos.</p>
    </div>
  );
}